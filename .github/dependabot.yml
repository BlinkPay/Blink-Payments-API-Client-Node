version: 2
updates:
  # Keeps the SHA-pinned actions in .github/workflows moving; Dependabot updates the SHA and the version comment together
  - package-ecosystem: github-actions
    directory: /
    schedule:
      interval: weekly
    commit-message:
      prefix: "chore"
